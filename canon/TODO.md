Stop emitting fn_ptr() into generated code.
Enforce lifetime emission when struct field contains &.
Enforce trait + impl signature parity.
Emit only one canonical import per symbol.
Fully qualify internal calls (crate::RuntimeBytecode::interp::...) instead of guessing.
