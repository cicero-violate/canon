pub(crate) struct IncludeVisitor<'a> {
    pub(crate) base_path: &'a Path,
    pub(crate) targets: &'a mut Vec<PathBuf>,
}


pub(crate) struct IncludeVisitor<'a> {
    pub(crate) base_path: &'a Path,
    pub(crate) targets: &'a mut Vec<PathBuf>,
}


pub(crate) struct IncludeVisitor<'a> {
    pub(crate) base_path: &'a Path,
    pub(crate) targets: &'a mut Vec<PathBuf>,
}


pub(crate) struct IncludeVisitor<'a> {
    pub(crate) base_path: &'a Path,
    pub(crate) targets: &'a mut Vec<PathBuf>,
}
